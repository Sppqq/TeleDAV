# 🚀 TeleDAV - Быстрый старт

## ⏱️ 5 минут до работающей системы

### Шаг 1: Подготовка учетных данных (2 минуты)

#### Получить Bot Token:
1. Откройте Telegram и найдите @BotFather
2. Напишите `/newbot`
3. Дайте имя боту (например, "TeleDAVBot")
4. Дайте username (должен быть уникальным, например, "MyTeleDAVBot")
5. Скопируйте полученный токен вида: `123456789:ABCDefghIjklmnOPQrstuvwxyz`

#### Получить Chat ID:
1. Создайте приватную Telegram группу (или используйте существующую)
2. **ВАЖНО:** Включите режим "Темы" (Forums) в параметрах группы
3. Добавьте бота в группу как администратора
4. Найдите бота @getidsbot в Telegram
5. Перенаправьте его в вашу группу (Reply)
6. Скопируйте Chat ID (отрицательное число, например, `-1001234567890`)

### Шаг 2: Клонирование и конфигурация (1 минута)

```bash
# Клонируем репозиторий
git clone https://github.com/Sppqq/TeleDAV.git
cd TeleDAV

# Копируем пример конфигурации
cp .env.example .env

# Открываем для редактирования
nano .env
```

### Шаг 3: Редактирование .env (1 минута)

```env
# Вставьте ваши данные
BOT_TOKEN=123456789:ABCDefghIjklmnOPQrstuvwxyz
CHAT_ID=-1001234567890
DAV_USERNAME=admin
DAV_PASSWORD=YourSecurePassword123
DAV_HOST=0.0.0.0
DAV_PORT=5555
```

**Ctrl+X** → Y → Enter (сохраняем в nano)

### Шаг 4: Запуск (1 минута)

#### Вариант A: Docker (рекомендуется)
```bash
docker compose up --build
```

#### Вариант B: Локально
```bash
python3 -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
python -m teledav.main
```

## ✅ Проверка работы

Если вы видите:
```
============================================================
🚀 TeleDAV - WebDAV Server with Telegram Storage
============================================================
Инициализирую базу данных...
✅ База данных успешно инициализирована
============================================================
📡 WebDAV сервер запущен на 0.0.0.0:5555
👤 Пользователь: admin
🔐 Используется Basic Auth
============================================================
```

**Отлично! Сервер работает!** ✨

## 🔗 Подключение к WebDAV

### 1. macOS/Linux (davfs2)

```bash
sudo apt-get install davfs2
mkdir ~/teledav
sudo mount -t davfs http://localhost:5555/ ~/teledav
# Username: admin
# Password: YourSecurePassword123
```

Готово! Папка `~/teledav` будет синхронизирована с вашей группой Telegram.

### 2. Windows (Native)

1. Откройте Проводник
2. Правый клик на "Этот компьютер"
3. Выберите "Подключить сетевой диск"
4. Папка: `http://localhost:5555/`
5. ☑️ Подключиться с использованием разных учетных данных
6. Введите:
   - Пользователь: `admin`
   - Пароль: `YourSecurePassword123`

### 3. Nextcloud

1. Зайдите в Nextcloud → Settings → External storages
2. Добавьте новое хранилище:
   - **Folder name:** "Telegram"
   - **External storage:** WebDAV
   - **Authentication:** Username and password
   - **URL:** `http://localhost:5555/`
   - **Username:** `admin`
   - **Password:** `YourSecurePassword123`
3. Нажмите ✓ для проверки соединения

## 📝 Первое использование

### Создание папки:
```
WebDAV → New Folder → "Documents"
↓
Приложение создает тему в Telegram с названием "Documents"
```

### Загрузка файла:
```
Документ.pdf (100 МБ) → в папку Documents
↓
Автоматически разбивается на 3 части (49.9, 49.9, 0.2 МБ)
↓
Загружается в Telegram в тему "Documents"
↓
Сохраняется в БД как 1 файл из 3 частей
```

### Скачивание файла:
```
Документ.pdf ← Скачать
↓
Части загружаются параллельно из Telegram
↓
Объединяются в один файл
↓
Отправляется клиенту
```

## 🐛 Частые проблемы

| Проблема | Решение |
|----------|---------|
| "Invalid credentials" | Проверьте DAV_USERNAME и DAV_PASSWORD в .env |
| "Topic not created" | Убедитесь, что группа имеет режим "Темы" включен |
| "Connection refused" | Проверьте что сервер запущен на порту 5555 |
| "Bot failed" | Проверьте BOT_TOKEN и CHAT_ID, бот должен быть администратором |
| "Database error" | Удалите `teledav.db` и перезагрузитесь |

## 📊 Архитектура в трех строках

```
Клиент (Nextcloud) ↔ WebDAV (FastAPI) ↔ Telegram (Хранилище)
                                ↓
                          SQLite (Метаданные)
```

## 🔐 Безопасность

- Используйте **HTTPS** в production (через nginx reverse proxy)
- Измените пароль DAV_PASSWORD на что-то сложное
- Используйте **приватную группу** в Telegram
- Регулярно **меняйте пароли**

## 📚 Дополнительная информация

- [README.md](README.md) - полная документация
- [IMPLEMENTATION.md](IMPLEMENTATION.md) - техническая информация
- [PROJECT_STRUCTURE.md](PROJECT_STRUCTURE.md) - описание файлов проекта

## 💡 Примеры использования

### Через терминал (Linux/macOS):

```bash
# Создать папку
mkdir ~/teledav/MyDocs

# Скопировать файлы
cp /path/to/file.pdf ~/teledav/MyDocs/

# Скачать файлы
cp ~/teledav/MyDocs/file.pdf /local/path/

# Синхронизировать с rsync
rsync -av ~/important_files/ ~/teledav/Backup/
```

### Через Nextcloud:

1. Откройте Nextcloud в браузере
2. Зайдите во "Внешние хранилища"
3. Увидите "Telegram" как обычную папку
4. Drag & Drop файлы
5. Все синхронизируется автоматически!

## 🎯 Следующие шаги

1. ✅ Убедитесь, что базовая конфигурация работает
2. ✅ Протестируйте загрузку/скачивание файлов
3. ✅ Проверьте файлы в Telegram группе (должны быть в темах)
4. ✅ Подключите к Nextcloud или другому WebDAV клиенту
5. ✅ Настройте бэкап или синхронизацию

## 📞 Получить помощь

- 🐛 [Создать Issue](https://github.com/Sppqq/TeleDAV/issues)
- 💬 [Обсуждение](https://github.com/Sppqq/TeleDAV/discussions)
- ⭐ Если нравится - дайте звезду проекту!

---

**Готово!** Вы настроили полнофункциональный WebDAV сервер с Telegram в качестве хранилища. 🎉

**Версия:** 1.0.0  
**Статус:** Готово к production
