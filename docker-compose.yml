services:
  teledav:
    build: .
    container_name: teledav
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${DAV_PORT}:${DAV_PORT}"
    volumes:
      - ./teledav.db:/app/teledav.db